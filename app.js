// Lógica base da aplicação
document.addEventListener('DOMContentLoaded', () => {
    console.log("App Turma inicializada com sucesso!");
    // Aqui no futuro vamos colocar o código de ligação ao Firebase
});
